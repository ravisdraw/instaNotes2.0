import { ArrayToWordsPipe } from './array-to-words.pipe';

describe('ArrayToWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayToWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
